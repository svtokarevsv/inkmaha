@layer base {
  .wrapper {
    min-height: 100vh;
    @apply my-0 pt-16 mx-auto flex justify-center items-start flex-col;
  }

  main {
    @apply flex flex-col flex-auto w-full mx-auto mt-16 pt-5;
  }
}
